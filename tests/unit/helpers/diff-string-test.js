import {
  diffString
} from 'wordset/helpers/diff-string';

module('DiffStringHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = diffString(42);
  ok(result);
});
