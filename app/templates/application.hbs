{{partial "menu"}}

<div class="main-content">
  <header class="header">
    {{#link-to "index"}}
      <div class="logo"></div>
    {{/link-to}}

    {{render "search"}}

    <div class="togglers">

      <a {{action 'toggleChat'}} class="chat-toggler" alt="Chat">
        <span {{bind-attr class="hasChatAlert :ion-android-chat showChat"}}></span>
        <!-- <span {{bind-attr class="hasChatAlert:ion-chatbox-working:ion-chatbox showChat"}}></span> -->
      </a>
      <a {{action 'toggleMenu'}} class="nav-toggler" alt="Menu">
        <span class="ion-android-apps"></span>
      </a>
    </div>
  </header>

  {{flash-message}}

  {{#view "main-area"}}
    <aside {{bind-attr class=":messages showChat:open:closed"}}>
      {{render "messages.index"}}
    </aside>
    <menu {{bind-attr class=":content showChat"}}>
      {{outlet}}
    </menu>
  {{/view}}
</div>
