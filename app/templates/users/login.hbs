<form {{submit "authenticate"}}>
  <div class="sign-module">
    <h3> Sign In </h3>
    {{#if errorMessage}}
      <div class="login-error">
        <span class="ion-android-alert"></span>
        {{errorMessage}}
      </div>
    {{/if}}
    <div class="username">
      <label> Username
      {{input value=identification name="username" type="text" autofocus="autofocus"}}
      </label>
    </div>
    <div class="password">
      <label> Password
      {{input type="password" name="password" value=password}}
      </label>
    </div>
    <button {{action "authenticate"}}> Sign In </button>

    <div class="aside">
      Don't have an account? Register {{#link-to 'users.new'}}here{{/link-to}}.
      Or, if you forgot your password, we can {{#link-to 'users.forgot-password'}}reset it{{/link-to}}.
    </div>
  </div>
</form>
