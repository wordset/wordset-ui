{{partial "word/entries"}}

{{#if session.isAuthenticated}}
  {{#if showMeaningProposal}}
    {{render "word/new-meaning" newMeaningProposal}}
  {{else}}
    <button {{action 'toggleShowAddMeaning'}}>
      Propose New Meaning
    </button>
  {{/if}}
{{/if}}
