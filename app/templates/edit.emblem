.wrapper
  .wordWrap
    .word
      = name
    .edit
      a.edit cancel

  = outlet

  textarea.json

  .reason-input
    label for="reason" Reason
    input id="reason"

  button.save-suggestion
    | Save Suggestion
    span.ion-chevron-right
