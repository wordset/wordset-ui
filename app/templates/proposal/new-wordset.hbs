{{#if isEditing}}
  {{partial "proposal/edit-new-word"}}
{{else}}
  <div class="word">
    {{model.wordName}}
  </div>
  {{#each model.meanings as |meaning|}}
    {{render "proposal/new-wordset-submeaning" meaning}}
  {{/each}}
{{/if}}
