{{label-box label=parentLabel
            selectedLabels=selectedLabels
            type="parent"
            isChecked=parentChecked
            disabled=disabledParent}}

{{#each children as |label|}}
  {{label-box label=label
              selectedLabels=selectedLabels
              type="child"
              parentState=parentChecked}}
{{/each}}
