{{label-box label=parentLabel selectedLabels=selectedLabels type="parent" useId=useId}}

{{#each label in parentLabel.children}}
  {{label-box label=label selectedLabels=selectedLabels type="child" useId=useId}}
{{/each}}
