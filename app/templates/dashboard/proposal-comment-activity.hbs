<div class="activity">
  {{#link-to "proposal.index" model.proposalId}}
    <span class="ionicon ion-chatbox"></span>
    {{model.userId}} commented on the proposal for
    <span class="activity-word">{{model.wordName}}</span>.
  {{/link-to}}
</div>
